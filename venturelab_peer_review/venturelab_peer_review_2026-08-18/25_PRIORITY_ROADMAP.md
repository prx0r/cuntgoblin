# Priority Roadmap

## Phase 0 — executable truth

1. purge tracked venv/cache,
2. package + lock dependencies,
3. create composition root,
4. fix REST startup,
5. official MCP,
6. restore/remove broken global runtime,
7. source adapters,
8. correct Hermes Kanban adapter,
9. one root test command,
10. green clean-checkout Actions.

**Exit:** repo boots and CI independently proves it.

## Phase 1 — one durable slice

```text
opportunity
→ durable research job
→ Hermes researcher
→ evidence normalization
→ verifier
→ score
→ data-oracle build
→ deterministic verification
→ certificate
```

## Phase 2 — HotSwap execution

Wire route planning to actual executor, fallback, verification reward, cost/quota/provider telemetry.

## Phase 3 — Agent Factory

Connect AgentHub to real architectures/evals.

## Phase 4 — plugin contract

Refactor API + Agent as plugins. Add Connector Factory as the second implementation.

## Phase 5 — Commerce

Only after connectors, QA, approvals and telemetry are reliable.

## Phase 6 — learning

Route/factory/skill outcomes. Only then learned routing/RL-style optimization.
