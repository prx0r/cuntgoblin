# EndpointTruth MVP — runtime dependencies
fastapi>=0.110
uvicorn[standard]>=0.29
httpx>=0.27
pydantic>=2.6
pyyaml>=6.0
# optional (falls back to hand-rolled JSON-RPC MCP if absent)
mcp>=1.0
# dev / tests
pytest>=8.0