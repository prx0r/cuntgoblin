# COPY INTO THE VENTURELAB / DELL AGENT

You are integrating HotSwap across `prx0r/dell` and `prx0r/cuntgoblin`.

Read every file in this pack before editing.

## Mission

Make every VentureLab LLM task automatically run on the cheapest economically sensible
model-route that satisfies its task contract, preferring free capacity, while preserving
quality through objective evaluation and escalation.

## Do not rebuild

- provider SDK gateway → LiteLLM
- Hermes agent loop
- OpenRouter provider router
- Dell pricing/evidence ontology

Build only the missing intelligence/control plane.

## First action — Dell

Verify every issue in `01_DELL_FINAL_PEER_REVIEW.md`.

Do NOT trust the current final PASS certificate.
In particular inspect:
- 9/10 mutation PASS
- uncalled schema check
- budget enforcement
- missing output price
- endpoint construction
- confidence vs coverage
- invented missing-metric scores
- context-as-quality

Fix and recertify.

## Second action — Factory TaskSpec

Add explicit TaskSpec metadata to every agentic factory stage.
Never route a release certifier using the same default policy as source extraction.

## Third action — LiteLLM

Wire one centralized gateway.
Secrets stay outside HotSwap.
Tag calls for factory/product/task/run/plan.

## Fourth — deterministic HotSwap

Implement the reference semantics first.

Do not add neural routing yet.

## Fifth — quotas

This is mandatory before claiming free-first is real.

Track multiple independent quota windows and reservations.

## Sixth — Hermes

Generate per-task model/fallback configuration from HotSwap plans.
Do not globally mutate defaults for parallel workers.

## Seventh — outcomes

A Hermes process exiting successfully is not task success.
Use factory evaluator/certification results.

## Eighth — shadow and rollout

Prove savings at fixed quality before enabling broadly.

## Advanced algorithms

Only after real data:
- task-cell Beta posteriors
- bounded Thompson sampling
- contextual routing
- conformal/verified cascades

Every advanced policy must beat the previous simpler baseline.

## Required final outputs

- corrected Dell certificate
- HotSwap code
- TaskSpec integration
- LiteLLM config/integration docs
- Hermes runtime adapter
- quota ledger
- account opportunity queue
- router benchmark
- live factory run artifacts
- exact before/after Git SHAs
- known limitations
- GO/NO-GO

If the complex router does not outperform deterministic free-first ECPS,
ship the deterministic router.

Truth over novelty.
