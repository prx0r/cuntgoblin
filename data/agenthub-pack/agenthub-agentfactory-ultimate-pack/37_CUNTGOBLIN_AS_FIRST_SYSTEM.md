# First Canonical System — VentureLab Itself

Use `cuntgoblin` as AgentHub's first serious architecture.

Classify:

```text
runtime:
Hermes

families:
hierarchical
planner-worker
persistent-factory
worker-verifier

system type:
autonomous product/R&D factory
```

Observed structural mechanisms include:
- persistent task decomposition
- worker dispatch
- independent verification/certification
- artifact/run logging
- product builders
- market/opportunity subsystems
- HotSwap integration work

## First benchmark experiment

Create variants:

A. single Hermes agent
B. planner → one worker
C. planner → parallel workers
D. planner → workers → certifier
E. current VentureLab architecture

Freeze:
- model policy
- tools
- target task set

Run AH-FACTORY and AH-CODE.

This produces the first architecture-specific evidence for the registry.
