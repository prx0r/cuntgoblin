# COPY INTO THE CUNTGOBLIN AGENT

You are implementing AgentHub + Agent Factory + Architecture Benchmark Lab.

Baseline reviewed:
`22094efe30dc1a38d8e66ab545f543dd6deb3d93`

Read this entire pack first.

## Mission

Close the loop:

Market Intelligence
→ Opportunity
→ Solution Lab
→ ArchitectureNeed
→ reuse/fork/synthesize autonomous architecture
→ HotSwap model slots
→ Hermes execution
→ architecture benchmark
→ AgentHub registry/lineage
→ architecture learning/evolution.

## Mandatory correction

Current AGENTS.md hardcodes mimo v2.5.

Replace this with TaskSpec/HotSwap/Dell model selection.
Do not leave competing model-selection doctrines.

## Critical rules

1. Repo != AgentSystem != ArchitectureBuild != Installation != Run.
2. Benchmark pinned ArchitectureBuild, not repo name.
3. Hard requirements before architecture ranking.
4. UNKNOWN cannot satisfy hard capability.
5. Popularity != quality.
6. Architecture-controlled benchmarks freeze model/tool policy.
7. Simulation != real task performance.
8. One successful novel build does not become reusable architecture automatically.
9. Benchmark evaluator/gold is immutable during candidate search.
10. Evolution candidate cannot modify provenance/sandbox/evaluator.
11. Start with simple baselines.
12. Every architecture mutation has semantic lineage.
13. HotSwap resolves model slots; architecture specifies requirements.
14. Verifier independence must be real, not same context pretending to be a reviewer.
15. External AgentBeats/A2A compatibility is integration, not a replacement for AgentHub.

## First real system

Ingest current VentureLab/cuntgoblin.

Produce its truthful manifest from implementation evidence.

## First benchmark

Compare under SAME model policy:

A. single Hermes agent
B. planner-worker
C. planner-parallel-workers
D. planner-worker-verifier
E. current VentureLab architecture

Use native factory/code tasks with deterministic gates where possible.

Report:
- success
- lower bound
- cost/success
- wall time
- model calls/tokens
- recovery
- complexity

## Agent Factory proof

Create a deliberately nontrivial ArchitectureNeed.

If existing fit <.62:
- synthesize experimental candidates
- simulation screen
- real benchmark survivors
- retain Pareto archive
- run winner on solution

Correct output may be:
"winner remains experimental".

Do NOT fake promotion.

## Advanced work

Only after benchmark integrity:
- MaAS-style task-conditioned supernet
- DGM/Shinka archive evolution

Do not start Hyperagent/meta-self-modification until end-to-end system is proven.

## Final report

Create:
`data/agenthub/runs/<id>/`
and
`AGENTHUB-FINAL.md`

Include:
- before/after SHA
- architecture manifests
- doctor results
- controlled benchmark
- failure benchmark
- lineage
- Agent Factory search archive
- HotSwap integration
- A2A smoke
- promotion decisions
- limitations
- GO/NO-GO
